kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: apacheentity-management-dev
  namespace: dev
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: apacheentity-management-dev
  resources:
    requests:
      storage: 1Gi
