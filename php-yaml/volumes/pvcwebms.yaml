kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: apachewebms-dev
  namespace: dev
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: apachewebms-dev
  resources:
    requests:
      storage: 1Gi
