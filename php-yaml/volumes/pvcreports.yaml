
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: apachereports-dev
  namespace: dev
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: apachereports-dev
  resources:
    requests:
      storage: 1Gi
