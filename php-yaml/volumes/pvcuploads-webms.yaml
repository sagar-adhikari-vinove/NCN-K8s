kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: webms-uploads-dev
  namespace: dev
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: webms-uploads-dev
  resources:
    requests:
      storage: 1Gi
