kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: logfilevbo-dev
  namespace: dev
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: logfilevbo-dev
  resources:
    requests:
      storage: 1Gi
