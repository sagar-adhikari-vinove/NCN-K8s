apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: apacheentity-management-dev-new
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: "default-storage-class-1"
